export enum UserRole {
    NONE = 'none',
    STUDENT = 'student',
    TA = 'ta',
    ADMIN = 'admin',
}

export enum LabLogAction {
    CREATE = 'create',
    UPDATE = 'update',
    DELETE = 'delete',
}

export enum LabLogCategory {
    LAB = 'lab',
    SKELETON = 'skeleton',
    FILENAME = 'filename',
}
